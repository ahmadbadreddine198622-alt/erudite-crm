{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "brn": {
      "type": "string",
      "description": "Broker registration number"
    }
  },
  "required": [
    "role"
  ]
}