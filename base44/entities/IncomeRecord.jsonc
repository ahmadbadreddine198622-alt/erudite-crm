{
  "name": "IncomeRecord",
  "type": "object",
  "properties": {
    "invoice_id": {
      "type": "string",
      "description": "Reference to Invoice entity"
    },
    "agent_id": {
      "type": "string",
      "description": "Reference to Agent (User) entity"
    },
    "recognized_amount": {
      "type": "number"
    },
    "period": {
      "type": "string",
      "description": "Accounting period in YYYY-MM format"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "recognized",
        "reconciled"
      ],
      "default": "pending"
    },
    "description": {
      "type": "string",
      "description": "Optional description or notes about this income record (max 1000 chars)",
      "maxLength": 1000
    }
  },
  "required": [
    "invoice_id",
    "agent_id",
    "recognized_amount",
    "period",
    "status"
  ]
}